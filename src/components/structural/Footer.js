import React from 'react';
//import { Link } from 'react-router-dom';

const Footer = (props) => {
    return(
        <footer>
            <span>Footer</span>
        </footer>
    );
}

export default Footer;